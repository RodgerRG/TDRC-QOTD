import { createStyles, makeStyles } from "@material-ui/core";

const QuoteListPageStyles = makeStyles(createStyles({
    wrapper: {
        minHeight: '88vh',
        backgroundColor: '#252525'
    }
}));

export default QuoteListPageStyles;